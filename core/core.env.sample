# =============================================
# 🌐 WP Docker v2 - Core Environment Sample
# ---------------------------------------------
# Đây là file mẫu. Khi init.sh chạy lần đầu, sẽ tạo core.env từ file này.
# Các biến này được dùng để thiết lập cấu trúc dự án và runtime.
# =============================================

# Tên dự án (dùng trong docker compose: `name:`)
PROJECT_NAME=wpdocker

# Đường dẫn logic mà toàn bộ hệ thống hiểu là nơi cài đặt (dùng trong template)
INSTALL_DIR=/opt/wp-docker

# Image dùng cho container Python runtime
CORE_RUNTIME_IMAGES=python:3.10-slim

# Tên container runtime Python (được gọi bằng docker exec)
CORE_RUNTIME_CONTAINER_NAME=wpdocker-python-runtime

# Bật chế độ DEV (nếu true thì sẽ mount source thực)
DEV_MODE=false